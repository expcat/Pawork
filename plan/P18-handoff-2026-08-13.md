# Phase 18 交接记录（2026-08-13）

本记录是本轮中途收尾的事实清单。Phase 18 **尚未完成**；下一位 Agent 应以当前 `main`、本文件、各 P18 计划状态和实际测试结果为准，不沿用聊天中的乐观结论。

## 基线与合并

- 已从 `origin/main` 快进合并 P17 提交 `4351dddec2ce24cf84ab41f019b83226b763aec2`。
- 合并前 P18 工作以 `codex-p18-before-p17-merge-2026-08-13` 保存为 recovery stash；冲突解决后该 stash 仍保留，不应直接 apply 到新工作树。
- P17 schema v7 与 P18 identity/binding schema v8/v9 已按顺序合并，`CURRENT_SCHEMA_VERSION = 9`。

## 当前成熟度

| 范围 | 当前事实 | 下一步 |
| --- | --- | --- |
| P18-1/2/4/5/6/7/8/10 | 已实现并完成相应 L1/定向验证 | 只在 P18-15 做功能簇门禁 |
| P18-3 | 核心模型/迁移已验证 | 随 P18-14 完成生产宿主接线 |
| P18-9 | policy/RBAC/run+agent admission/GUI tenant scope 已接入 | 补并发 spawn 压力、恶意 pool lease 错配回归，再做 GLM reviewer |
| P18-11 | 独立 crate 只有官方协议基线骨架 | 实现 wire/contract/adapter/map/host 与完整 golden；之后接入根 workspace |
| P18-12 | 独立 Claude adapter crate 已全绿 | 共享 identity 契约、根 workspace、app-service/ledger/audit/protected-blob 生产接线 |
| P18-13 | `audit-log` store/query/export 与部分 app-service 桥已实现 | 补完整事件面、QuotaAlert、OTel/SIEM、WebScrape 收口和 reviewer |
| P18-14 | Provider registry 与原子 reload 已实现 | 实现 reconciler/probe/binding migration/quota targets/生产 composition 与故障注入 |
| P18-15 | 未开始 | 所有前置完成后，以独立 target 执行 L2 contract/security/golden/migration/rollback gate |

## 新 crate 的接线状态

- `audit-log` 已加入根 workspace，并由 app-service 使用。
- `client-claude-gateway` 与 `client-codex-app-server` 当前有自己的 `[workspace]`，故意保持为独立可测试 crate；下一位 Agent 完成共享契约/宿主接线时再删除嵌套 `[workspace]` 并加入根 members。
- 不要把 Codex crate 的 smoke test 误判为 P18-11 验收：Adapter 本体和 golden fixtures 尚未实现。

## 已有验证证据

- `cargo test -p provider-control binding`：34 passed。
- `cargo test -p session-store binding`：9 passed。
- `cargo test -p app-service --test tenant_policy`：14 passed；`cargo test -p app-service --lib`：99 passed。
- `cargo test -p gui-server --lib`：22 passed。
- `cargo test -p orchestration --lib`：78 passed。
- `cargo test -p audit-log`：5 passed。
- `cargo check -p app-service --tests`：通过。
- Claude 独立 crate：55 tests passed，Clippy `-D warnings`、fmt check 通过。
- Codex 独立 crate：1 smoke test passed。
- Provider registry：`cargo check -p provider-control` 通过，registry 定向测试 2 passed。

## 推荐续作顺序

1. P18-9 两组安全回归与 reviewer，先冻结 tenant/lease/agent enforcement。
2. P18-11 Adapter 本体与 golden；P18-12 共享契约及生产宿主接线。
3. P18-13 补齐事件面与 exporter；P18-14 完成 reconciler/生产 composition。
4. 同步各计划验收框与 feature 文档，最后执行 P18-15 L2。未命中 P18-15 前不要机械运行 workspace full gate。

**未运行**：Workspace Full Gate。原因：本轮按用户要求中途收尾，P18-11/13/14/15 仍有明确未完成项。
