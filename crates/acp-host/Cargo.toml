[package]
name = "acp-host"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
description = "ACP (Agent Client Protocol) host adapter: protocol translation to canonical core-api"

[dependencies]
agent-domain = { path = "../agent-domain" }
app-service = { path = "../app-service" }
async-trait.workspace = true
audit-log = { path = "../audit-log" }
client-adapter-api = { path = "../client-adapter-api" }
core-api = { path = "../core-api" }
serde.workspace = true
serde_json.workspace = true
subscription-hub = { path = "../subscription-hub" }
thiserror.workspace = true
tokio = { version = "1", features = ["rt", "sync", "macros", "time"] }
tracing.workspace = true

[dev-dependencies]
provider-api = { path = "../provider-api" }
session-store = { path = "../session-store" }
tempfile.workspace = true
test-support = { path = "../test-support" }
