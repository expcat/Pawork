[package]
name = "teams"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
authors.workspace = true
description = "Pawork P17-6 Agent Teams：team 生命周期 / shared task board / 持久 mailbox / presence / 受控 peer messaging / plan approval"

[dependencies]
agent-domain = { path = "../agent-domain" }
orchestration = { path = "../orchestration" }
plan-service = { path = "../plan-service" }
parking_lot.workspace = true
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true

[dev-dependencies]
agent-domain = { path = "../agent-domain" }
orchestration = { path = "../orchestration" }
serde_json.workspace = true
