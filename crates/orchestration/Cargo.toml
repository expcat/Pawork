[package]
name = "orchestration"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
authors.workspace = true
description = "Pawork Phase 12 Multi-Agent 编排：Supervisor/Worker 生命周期、任务图、worktree 隔离、预算、结果聚合与取消树"

[dependencies]
agent-domain = { path = "../agent-domain" }
git-service = { path = "../git-service" }
diff-service = { path = "../diff-service" }
provider-control = { path = "../provider-control" }
tenant-service = { path = "../tenant-service" }
usage-ledger = { path = "../usage-ledger" }
async-trait.workspace = true
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
tokio = { workspace = true, features = ["sync", "rt", "macros"] }
tracing.workspace = true
blake3.workspace = true

[dev-dependencies]
test-support = { path = "../test-support" }
tokio = { workspace = true, features = ["rt", "macros", "rt-multi-thread"] }
tempfile.workspace = true
