[package]
name = "agent-sdk"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
description = "Pawork Rust Agent SDK：连接 pawork Host 的 client（不嵌入 Core）"

[dependencies]
agent-domain = { path = "../agent-domain" }
async-trait.workspace = true
core-api = { path = "../core-api" }
headless-json = { path = "../headless-json" }
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
tokio = { workspace = true, features = ["rt", "rt-multi-thread", "process", "io-util", "sync", "macros", "time"] }

[dev-dependencies]
tempfile.workspace = true
